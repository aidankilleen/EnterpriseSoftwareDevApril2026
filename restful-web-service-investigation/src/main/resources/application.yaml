spring:
  application:
    name: restful-web-service-investigation
  datasource:
    url: jdbc:sqlite:C:\\work\\training\\java\\users.db
    driver-class-name: org.sqlite.JDBC
  jpa:
    database-platform: org.hibernate.community.dialect.SQLiteDialect